# @ruplot/core

## 0.1.0

### Initial public release

- Chart session, option classifier, external stores, sync registry, DataPlane
- `patchSeries`, slotted hooks, slotted axis formatters (`setUserAxes`)
- `createDataWorker`, streaming helpers, plugin runtime
